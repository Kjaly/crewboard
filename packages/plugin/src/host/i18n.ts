export type HostLang = 'en' | 'ru'
// Persisted billing labels are legacy enum values consumed by client settings; keep their wire values.
export const BILLING_SUBSCRIPTION = 'подписка'
export const BILLING_PROMO = 'промо'
export const PROVIDER_OTHER = 'Другие'
type Vars = Record<string, string | number>

// This host-only dictionary follows the client's t(key, vars) shape; it stays independent so the
// host bundle does not pull in the browser dictionary, React, or client runtime.
const en: Record<string, string> = {
  'tools.planDraft': 'Store a proposed plan graph as a draft and return machine-readable checks. This never approves or runs it. `draft` must match the PlanDraft JSON Schema of the parameter exactly; each decision is one plain string. Example: {example}',
  'tools.draftJobs': 'List draft jobs started from a spec that are still running, need repair (the worker answer did not match the PlanDraft schema) or failed. With `job`, return that job with its validator findings and the raw worker answer.',
  'tools.draftJobRepair': 'Act on a draft job that needs repair or failed: `repair` launches a worker run with the raw answer and the validator findings (or reruns the request when there is no answer); `discard` drops the job. To fix the JSON yourself, call orchestra_plan_draft with the corrected draft and then discard the job.',
  'tools.draftRefused': 'The draft does not match the PlanDraft schema; nothing was stored. Fix these findings and call again: {findings}',
  'tools.taskUpsert': 'Create or update a task (title, kind, lane, deps, contract, status backlog|ready). kind=root is your own work (stand integration, processes, the owner\'s database): no worker runs it; the kind of an open task may be changed. Do not pass a worker: the preset the person chose decides. Acceptance is human-only.',
  'tools.workerParam': 'Leave empty: the preset decides. Only a worker of the effective preset for the task class is accepted; anything else is refused. "auto" clears an assignment. If you think another worker is needed, ask the person.',
  'tools.run': "Launch a worker on a ready task: preflight, worktree from the recipe, baseline, then the worker. Returns the run id and worktree. Do not pass `agent`: the task's assigned worker runs, else the preset order for the task class.",
  'tools.agentParam': 'Leave empty: the preset decides. Only a worker of the effective preset for the task class is accepted; anything else is refused. If you think another worker is needed, ask the person.',
  'actions.draftApprove': 'Approve draft {id} as a new plan with {count} tasks? Review the draft and findings first.',
  'actions.draftApproveOk': 'Approve plan',
  'actions.draftBlocked': 'Draft {id} cannot be approved: fix the dependency cycle or the missing dependency first.',
  'presets.unknown': 'Unknown preset: {id}',
  'presets.builtinDelete': 'The built-in All workers preset cannot be deleted',
  'presets.invalid': 'Invalid preset',
  'actions.declined': 'Cancelled in confirmation dialog',
  'actions.accept.decision': 'Close decision {task}? This is your answer to the plan; waiting tasks will be unblocked. To decline, use “Send back”.',
  'actions.accept.negative': 'Close task {task} with a negative result?{why}',
  'actions.accept.disputed': 'Accept task {task}? Disagreement: {mismatch}',
  'actions.accept.unchecked': 'The orchestrator has not finished checking {task} — accept without it?\n\n',
  'tools.verify': 'Your check of finished work before it waits for the person (vr1). action=done with `note`: checked — a short summary of gates, stand and fixes; only then does the task wait for the person, with your note above Accept. The result carries the verdict the person will see; when it is disputed or no file changed, done is refused unless you pass `confirm: true`. action=return with `note`: send it back to its worker with the findings (a new run in the same worktree). action=take (optional) marks a finished task (status in_review) as being checked; on work you already checked it changes nothing — action=reopen takes it back from the person. Root tasks (kind root — your own work, never a worker\'s): action=start takes it in work, action=done with `note` and `report` finishes it and sends it to review. Decisions: action=done with `note` (options and your recommendation) and optional `report` prepares it — only then does it reach the person. Acceptance stays human-only.',
  'tools.alreadyChecked': 'Task {id} is already checked: nothing changed, it waits for the person. To check it again, use action=reopen.',
  'tools.doneUnconfirmed': 'Task {id} was not marked checked: the verdict the person will see is disputed or no file changed ({verdict}). If that is what you checked, repeat action=done with confirm: true; otherwise action=return with the findings.',
  'tools.attention': 'Return what waits on the person — the same «Needs you» list as `crewboard attention --json`: kind review (finished work; `checked` says whether you checked it), decision, attention (a failed, stalled or looping run, with the alarm), plan (another plan of the repository that waits). Empty means nothing waits.',
  'tools.planMismatch': 'This chat is bound to plan {bound} in {root}: it cannot act on plan {asked} in {repo}. Omit `plan` and `repo`, or ask the person to open the chat of that plan.',
  'actions.accept.normal': 'Accept task {task}? Changes are considered reviewed.',
  'actions.accept.batch': 'Accept {count} tasks? {clean} clean, {risky} at risk.\n\n{lines}',
  'actions.accept.batchRisks': '\n\nResults needing attention:\n{lines}',
  'actions.accept.root': 'Accept task {task}? The orchestrator did this work itself; its report is considered reviewed.',
  'actions.accept.ownUnchecked': 'The orchestrator has not reported or prepared {task} — accept without its check?\n\n',
  'actions.accept.batchUnchecked': '\n\nNot checked by the orchestrator — accept anyway?\n{lines}',
  'actions.accept.uncommitted': 'The copy of {task} holds {count} file(s) no commit carries: the task branch does not contain this result, and merging it would not bring it. After acceptance the task panel shows the commands to commit them on the task branch.\n\n',
  'actions.accept.batchUncommitted': '\n\nThe branch does not contain the result — files without a commit:\n{lines}',
  'actions.accept.batchUncommittedLine': '{count} file(s) not committed',
  'actions.accept.riskNegative': 'Negative result: {reason}',
  'actions.accept.riskDisputed': 'Disputed: {reason}',
  'verdict.reason.blocked': 'work is blocked',
  'verdict.reason.negative': 'a negative result was reported',
  'verdict.reason.run_failed': 'a result was claimed, but the last run failed or was cancelled',
  'verdict.reason.no_files': 'a result was claimed, but no files changed',
  'verdict.reason.report_missing': 'the task has a run, but no report',
  'verdict.reason.claim_missing': 'the report makes no explicit result claim',
  'actions.reject': 'Send task {task} back for rework: “{reason}”?',
  'actions.drop': 'Close task {task} as not needed: “{reason}”? It will not come back to the queue.',
  'actions.ok.drop': 'Close task',
  'actions.ok.accept': 'Accept', 'actions.ok.sendBack': 'Send back',
  'actions.ok.cancel': 'Cancel',
  'notify.title': 'crewboard · {task}',
  'notify.badge': 'Orchestration needs attention: {message}',
  'notify.stalled': 'No recent worker activity.', 'notify.failed': 'The worker run failed.',
  'notify.not_started': 'The worker has not started yet.', 'notify.loop': 'The worker may be repeating steps.',
  'notify.steer_no_effect': 'No activity after the last instruction.', 'notify.incomplete': 'The run ended without handing its work in: continue it.', 'notify.generic': 'A worker needs your attention.',
  'steer.delivered': 'Direction written to running worker {runId}. Check the feed for its effect.',
  'steer.refused': 'Direction refused: run {runId} is {state}. The correction is retained for relaunch.',
  'steer.abandoned': 'Direction {steerId} was not sent: {reason}.',
  'steer.failed': 'Direction could not be written to {runId}: {reason}.',
  'actions.error.unknown': 'Unknown error',
  'actions.defaultMismatch': 'the facts do not match the claim',
}
const ru: Record<string, string> = {
  'tools.planDraft': 'Сохранить предложенный граф как черновик и вернуть результаты проверки. Утверждения и запуска нет. `draft` должен точно соответствовать JSON Schema PlanDraft в параметре; каждое решение — одна строка. Пример: {example}',
  'tools.draftJobs': 'Показать задания черновиков из спецификации, которые ещё выполняются, требуют починки (ответ воркера не прошёл схему PlanDraft) или не удались. С `job` — это задание с замечаниями валидатора и сырым ответом воркера.',
  'tools.draftJobRepair': 'Действие над заданием черновика, которому нужна починка или которое не удалось: `repair` запускает воркера с сырым ответом и замечаниями валидатора (или повторяет запрос, если ответа нет); `discard` отбрасывает задание. Чтобы исправить JSON самому, вызовите orchestra_plan_draft с исправленным черновиком и отбросьте задание.',
  'tools.draftRefused': 'Черновик не соответствует схеме PlanDraft; ничего не сохранено. Исправьте замечания и вызовите снова: {findings}',
  'tools.taskUpsert': 'Создать или изменить задачу (title, kind, lane, deps, contract, status backlog|ready). kind=root — ваша собственная работа (интеграция на стенде, процессы, база владельца): воркер её не выполняет; тип открытой задачи можно менять. Воркера не указывайте: решает пресет, который выбрал человек. Приёмка — только человек.',
  'tools.workerParam': 'Оставьте пустым: решает пресет. Принимается только воркер из действующего пресета для класса задачи, иначе отказ. "auto" снимает назначение. Если нужен другой воркер — спросите человека.',
  'tools.run': 'Запустить воркера на готовой задаче: preflight, worktree по рецепту, базовый прогон, затем воркер. Возвращает id запуска и worktree. Не передавайте `agent`: запускается назначенный воркер задачи, иначе — по порядку пресета для класса задачи.',
  'tools.agentParam': 'Оставьте пустым: решает пресет. Принимается только воркер из действующего пресета для класса задачи, иначе отказ. Если нужен другой воркер — спросите человека.',
  'actions.draftApprove': 'Утвердить черновик {id} как новый план с {count} задачами? Сначала проверьте черновик и замечания.',
  'actions.draftApproveOk': 'Утвердить план',
  'actions.draftBlocked': 'Черновик {id} нельзя утвердить: сначала устраните цикл зависимостей или отсутствующую зависимость.',
  'presets.unknown': 'Неизвестный пресет: {id}',
  'presets.builtinDelete': 'Встроенный пресет «Все воркеры» удалить нельзя',
  'presets.invalid': 'Некорректный пресет',
  'actions.declined': 'Отменено в окне подтверждения',
  'actions.accept.decision': 'Закрыть решение {task}? Это ваш ответ плану — задачи, которые его ждут, разблокируются. Отклонить — кнопкой «Вернуть…».',
  'actions.accept.negative': 'Закрыть задачу {task} с отрицательным результатом?{why}',
  'actions.accept.disputed': 'Принять задачу {task}? Расхождение: {mismatch}',
  'actions.accept.unchecked': 'Оркестратор ещё не закончил проверку {task} — принять без неё?\n\n',
  'tools.verify': 'Ваша проверка законченной работы до того, как она попадёт к человеку (vr1). action=done с `note`: проверено — короткая сводка гейтов, стенда и правок; только после этого задача ждёт человека, ваша сводка — над кнопкой «Принять». В ответе — вердикт, который увидит человек; если он спорный или файлы не изменены, done отклоняется без `confirm: true`. action=return с `note`: вернуть воркеру с замечаниями (новый запуск в той же рабочей копии). action=take (необязательно) отмечает законченную задачу (статус in_review) как проверяемую; на уже проверенной работе ничего не меняет — action=reopen забирает её у человека. Root-задачи (kind root — ваша собственная работа, не воркера): action=start берёт её в работу, action=done с `note` и `report` завершает и отправляет на приёмку. Решения: action=done с `note` (варианты и ваша рекомендация) и необязательным `report` готовит решение — только тогда оно попадает к человеку. Приёмка — только человек.',
  'tools.alreadyChecked': 'Задача {id} уже проверена: ничего не изменилось, она ждёт человека. Проверить заново — action=reopen.',
  'tools.doneUnconfirmed': 'Задача {id} не отмечена проверенной: вердикт, который увидит человек, спорный или файлы не изменены ({verdict}). Если вы проверили именно это, повторите action=done с confirm: true; иначе action=return с замечаниями.',
  'tools.attention': 'Вернуть то, что ждёт человека, — тот же список «Нужны вы», что `crewboard attention --json`: kind review (законченная работа; `checked` — проверили ли вы её), decision, attention (упавший, зависший или зациклившийся запуск, с алармом), plan (другой план репозитория, который ждёт). Пусто — ничего не ждёт.',
  'tools.planMismatch': 'Этот чат привязан к плану {bound} в {root}: он не может работать с планом {asked} в {repo}. Не указывайте `plan` и `repo` или попросите человека открыть чат того плана.',
  'actions.accept.normal': 'Принять задачу {task}? Изменения считаются проверенными.',
  'actions.accept.batch': 'Принять {count} задач(и)? Чистых: {clean}, с риском: {risky}.\n\n{lines}',
  'actions.accept.batchRisks': '\n\nРезультаты, требующие внимания:\n{lines}',
  'actions.accept.root': 'Принять задачу {task}? Эту работу оркестратор сделал сам; его отчёт считается проверенным.',
  'actions.accept.ownUnchecked': 'Оркестратор ещё не отчитался по {task} и не подготовил её — принять без его проверки?\n\n',
  'actions.accept.batchUnchecked': '\n\nНе проверено оркестратором — всё равно принять?\n{lines}',
  'actions.accept.uncommitted': 'В копии {task} есть файлы без коммита ({count}): ветка задачи не содержит этот результат, и слияние его не принесёт. После приёмки панель задачи покажет команды, чтобы закоммитить их в ветке задачи.\n\n',
  'actions.accept.batchUncommitted': '\n\nВетка не содержит результата — файлы без коммита:\n{lines}',
  'actions.accept.batchUncommittedLine': 'не закоммичено файлов: {count}',
  'actions.accept.riskNegative': 'Отрицательный результат: {reason}',
  'actions.accept.riskDisputed': 'Спорно: {reason}',
  'verdict.reason.blocked': 'работа заблокирована',
  'verdict.reason.negative': 'получен отрицательный результат',
  'verdict.reason.run_failed': 'заявлен результат, но последний запуск завершился с ошибкой или отменой',
  'verdict.reason.no_files': 'заявлен результат, но изменённых файлов нет',
  'verdict.reason.report_missing': 'у задачи есть запуск, но отчёт отсутствует',
  'verdict.reason.claim_missing': 'в отчёте нет явного заявления о результате',
  'actions.reject': 'Вернуть задачу {task} на доработку: «{reason}»?',
  'actions.drop': 'Закрыть задачу {task} как ненужную: «{reason}»? В очередь она не вернётся.',
  'actions.ok.drop': 'Закрыть задачу',
  'actions.ok.accept': 'Принять', 'actions.ok.sendBack': 'Вернуть',
  'actions.ok.cancel': 'Отмена',
  'notify.title': 'crewboard · {task}',
  'notify.badge': 'Оркестрация, нужна реакция: {message}',
  'notify.stalled': 'Воркер давно не выполнял действия.', 'notify.failed': 'Запуск воркера завершился с ошибкой.',
  'notify.not_started': 'Воркер ещё не начал работу.', 'notify.loop': 'Возможно, воркер повторяет шаги.',
  'notify.steer_no_effect': 'После поправки нет активности.', 'notify.incomplete': 'Запуск закончился, не сдав работу: продолжите его.', 'notify.generic': 'Воркеру нужно ваше внимание.',
  'steer.delivered': 'Поправка записана работающему воркеру {runId}. Проверьте эффект в ленте.',
  'steer.refused': 'Поправка отклонена: запуск {runId} в состоянии {state}. Текст сохранён для перезапуска.',
  'steer.abandoned': 'Поправка {steerId} не отправлена: {reason}.',
  'steer.failed': 'Не удалось записать поправку в {runId}: {reason}.',
  'actions.error.unknown': 'Неизвестная ошибка',
  'actions.defaultMismatch': 'факты расходятся с заявлением',
}
export function hostT(lang: HostLang, key: string, vars: Vars = {}): string {
  return (lang === 'ru' ? ru[key] ?? en[key] : en[key])?.replace(/\{([\w]+)\}/g, (m, name: string) => vars[name] === undefined ? m : String(vars[name])) ?? key
}
export function hostLang(preference: unknown): HostLang { return preference === 'ru' ? 'ru' : 'en' }
